#ifndef WTOP_VIEW_PROC_H
#define WTOP_VIEW_PROC_H

#include "ui.h"

/* The process list screen: per-core/CPU + memory meters, the sortable process
 * table, filtering, tree view, tagging, and the kill flow. */
extern const ViewVTable VIEW_PROC_VTABLE;

#endif /* WTOP_VIEW_PROC_H */
